<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloud.system.mapper.UserMapper">


    <select id="selectByName" resultType="com.cloud.api.system.dto.UserLoginDTO">
        select u.id, u.username, u.password, u.nickname, u.email, u.phone, u.status, u.avatar
            from sys_user u
        where u.name = #{username}
    </select>
    <select id="selectRoleCodesByUserId" resultType="com.cloud.system.entity.Role">
        SELECT r.id, r.code
        FROM sys_user u
                 LEFT JOIN sys_user_role ur ON u.id = ur.user_id
                 LEFT JOIN sys_role r ON ur.role_id = r.id
        WHERE u.username = #{username}
    </select>

</mapper>
