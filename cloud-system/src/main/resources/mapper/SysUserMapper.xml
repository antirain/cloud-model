<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloud.system.mapper.SysUserMapper">
    <select id="selectRolesByUserId" resultType="com.cloud.system.entity.SysRole">
        SELECT r.id, r.role_code, r.role_name, r.description, r.status
        FROM sys_user u
                 JOIN sys_user_role ur ON u.id = ur.user_id
                 JOIN sys_role r ON ur.role_id = r.id
        WHERE u.id = #{userId}
    </select>
    <select id="selectPermissionByRoleIds" resultType="java.lang.String">
        SELECT m.menu_code
        FROM sys_menu m
        LEFT JOIN sys_role_menu rm ON rm.menu_id = m.id
        WHERE rm.role_id in
        <foreach item="item" index="index" collection="roleIds"
                 open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>
</mapper>
