server:
  port: 9004

spring:
  application:
    name: cloud-product
  config:
    import:
      - nacos:common-db.yaml
      - nacos:common-redis.yaml
      - nacos:cloud-product.yaml
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: public
        group: DEFAULT_GROUP
      config:
        server-addr: localhost:8848
        namespace: public
        group: DEFAULT_GROUP
        file-extension: yaml
        refresh-enabled: true